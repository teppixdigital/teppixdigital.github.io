# Production-only overrides.
# Keep this file free of secrets and environment-specific values in version control.

# Add your GA4 measurement ID here in your deployment pipeline as needed.
# Example:
# google_analytics: "G-XXXXXXXXXX"
